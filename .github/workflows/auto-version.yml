name: Auto Version
on:
  push:
    branches:
      - main

jobs:
  auto-version:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    uses: HalloftheGods/.github/.github/workflows/auto-version.yml@main
    with:
      plugin_slug: 'xophz-compass-xp'
